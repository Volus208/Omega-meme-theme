# Big funny Omega theme

To use this theme, add `THEME_NAME` and `THEME_REPO` arguments to your `make` command:
```bash
$ make THEME_REPO=https://github.com/Volus208/Omega-meme-theme.git THEME_NAME=omega_meme -j4
```
